Small tracked-caps marker for product descriptors, claims ("WORKS IN MINUTES") and status. Monochrome.

```jsx
<Badge variant="solid">New</Badge>
<Badge variant="outline">Temporary Effect</Badge>
```

Variants: `solid` (ink), `outline` (hairline), `plain` (muted text). Sizes `sm | md`.

import { ChiselComposition } from "@/components/chisel/ChiselComposition";

export const metadata = {
  title: "CHISEL — Contour Sculpt System · VIS MAJOR",
  description:
    "CHISEL is a warming contour cream worked in with a weighted steel tool — a paired system for a firmer, more defined look. Temporary, cosmetic, made in the UK by VIS MAJOR.",
};

export default function ChiselPage() {
  return <ChiselComposition />;
}
